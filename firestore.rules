rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Public read/write rules for summer camp planner (no authentication yet, as requested)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
